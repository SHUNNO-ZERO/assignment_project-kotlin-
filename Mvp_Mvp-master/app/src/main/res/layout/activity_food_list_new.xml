<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">


    <include
        android:id="@+id/toolbar_layout_new"
        layout="@layout/activity_toolbar_new" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView_new"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_layout_new"
        tools:listitem="@layout/activity_item_food_new" />

    <ProgressBar
        android:id="@+id/progress_new"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/toolbar_layout_new"
        app:layout_constraintStart_toStartOf="@+id/toolbar_layout_new"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_layout_new"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>


